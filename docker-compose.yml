version: '2'
services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: pass
      MYSQL_DATABASE: notes_app_dev
      MYSQL_USER: notes_app_admin
      MYSQL_PASSWORD: pass
    ports:
      - "3306:3306"
  app:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - ".:/notes_app"
    ports:
      - "3000:3000"
    depends_on:
      - db
    links:
      - db
    environment:
      NOTES_APP_DATABASE_PASSWORD: pass
